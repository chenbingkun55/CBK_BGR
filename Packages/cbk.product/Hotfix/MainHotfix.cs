using UnityEngine;

namespace CBK.Hotfix
{
    public class MainHotfix : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            Debug.Log("hello hotfix main 5, HybridCLR");
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}
