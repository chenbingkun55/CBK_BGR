namespace CBK.Product
{
    // 用餐类型
    public enum EatType
    {
        None = 0,
        Breakfast, // 早餐
        Lunch, // 午餐
        Dinner, // 晚餐
    }

    public enum DataType
    {
        None = 0,
        RecordData, // 记录数据
    }
}