using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using CBK;

public class PanelRecode : MonoBehaviour
{
    //
    [SerializeField] private Transform m_inputDate = default;
    [SerializeField] private Transform m_inputEatType = default;
    [SerializeField] private Transform m_inputEatTime = default;
    [SerializeField] private Transform m_inputMedicineType = default;
    [SerializeField] private Transform m_inputMedicineAmount = default;
    [SerializeField] private Transform m_inputNotice = default;


    // Start is called before the first frame update
    void Start()
    {
 
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void AddRecode()
    {

    }
}
